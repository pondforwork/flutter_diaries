class Diaries {
  String image;
  String title;
  String story;

  Diaries(this.image, this.title, this.story);
}

class NoteTable {
  static const String id = '_id';
  static const String image = 'image';
  static const String title = 'title';
  static const String story = 'story';
}
